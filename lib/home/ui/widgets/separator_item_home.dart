import 'package:flutter/material.dart';

class SeparatorItemHome extends StatelessWidget {
  const SeparatorItemHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 10,
      color: Color.fromARGB(141, 158, 158, 158),
    );
  }
}
